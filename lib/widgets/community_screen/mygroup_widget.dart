import 'package:flutter/material.dart';

class MyGroupWidget extends StatelessWidget {
  const MyGroupWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('mygroup'),
    );
  }
}
