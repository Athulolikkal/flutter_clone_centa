import 'package:flutter/material.dart';

class SearchedItemsList extends StatelessWidget {
  const SearchedItemsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Searched Items List');
  }
}
